# Backend Configuration
BACKEND_HOST=127.0.0.1
BACKEND_PORT=8000

# Database Directory
DATABASES_DIR=./databases

# OpenAlex API (no API key required, but email improves rate limits)
# OPENALEX_EMAIL=your-email@example.com
